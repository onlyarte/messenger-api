runtime: nodejs12
env: standard
service: default

instance_class: F1
automatic_scaling:
  max_instances: 1

env_variables:
  NODE_ENV: production
  APP_ENV: DEV
  DEFAULT_GCLOUD_PROJECT: remessage-dev
