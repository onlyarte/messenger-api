runtime: nodejs10
env: standard
service: default

instance_class: B1
basic_scaling:
  max_instances: 1

env_variables:
  NODE_ENV: production
  APP_ENV: PRODUCTION
  DEFAULT_GCLOUD_PROJECT: remessage-production
